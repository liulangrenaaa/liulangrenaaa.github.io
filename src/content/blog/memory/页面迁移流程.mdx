---
title: 页面迁移流程
date: 2023-02-28 19:00:40
tags:
    - migrate
    - page
    - memory
categories:
    - linux内核
slug: "memory/页面迁移流程"
---



## 页面迁移属性

页面迁移属性包含
```
enum migratetype {
	MIGRATE_UNMOVABLE,
	MIGRATE_MOVABLE,
	MIGRATE_RECLAIMABLE,
	MIGRATE_PCPTYPES,	/* the number of types on the pcp lists */
	MIGRATE_HIGHATOMIC = MIGRATE_PCPTYPES,
#ifdef CONFIG_CMA
	MIGRATE_CMA,
#endif
#ifdef CONFIG_MEMORY_ISOLATION
	MIGRATE_ISOLATE,	/* can't allocate from here */
#endif
	MIGRATE_TYPES
};
```

未分配的页面按照这些迁移类型 在 free_list 中 排列。

```
struct free_area {
	struct list_head	free_list[MIGRATE_TYPES];
	unsigned long		nr_free;
};
```

只有 `MIGRATE_MOVABLE` 类型的页面能进行迁移，

























































































参考[Page migration](https://docs.kernel.org/mm/page_migration.html)
参考[]()
参考[]()
参考[]()

